import {chromium} from '@playwright/test';
import assert from 'node:assert/strict';
const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://127.0.0.1:5173/projects',{waitUntil:'networkidle'});assert.equal(await p.locator('main > section').count(),4);assert.ok(await p.locator('.projects-banner img').evaluate(i=>i.complete&&i.naturalWidth>0));await p.screenshot({path:'test-results/projects-personal-banner.png'});await p.setViewportSize({width:390,height:844});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await p.goto('http://127.0.0.1:5173/',{waitUntil:'networkidle'});assert.equal(await p.locator('.project-testimonials__card').count(),3);assert.equal(await p.locator('.project-testimonials').getByText('Founder Silibrain LLC').count(),0);console.log('PASS: simplified projects, banner image, mobile width, three testimonials.');}finally{await b.close();}
