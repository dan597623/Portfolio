import {chromium} from '@playwright/test';
import assert from 'node:assert/strict';
const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://127.0.0.1:5173/',{waitUntil:'networkidle'});await p.locator('.project-testimonials').scrollIntoViewIfNeeded();assert.match(await p.locator('.project-testimonials__card.is-active').innerText(),/Ciaran Rooney/);await p.getByRole('button',{name:'Next testimonial',exact:true}).click();assert.match(await p.locator('.project-testimonials__controls').innerText(),/2 \/ 4/);await p.getByRole('button',{name:'Previous testimonial',exact:true}).click();await p.screenshot({path:'test-results/testimonial-carousel.png'});await p.setViewportSize({width:390,height:844});assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);console.log('PASS carousel next/previous and mobile width');}finally{await b.close();}
